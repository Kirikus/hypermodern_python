````{div}
:name: description
```{image} ../../_static/img/logo/ruff.svg?raw=true
:align: left
:class: img-bg-fill
```

### Using linters and formatters

[\[Reference\]](../materials/ruff)

Project uses `ruff` for formatting and linting, along with formatters for other file types (expand list as you add them to project).

Relevant section in `pyproject.toml` contain meaningful settings and rules.

Does not require automatic startup via hooks or similar techniques.
````
